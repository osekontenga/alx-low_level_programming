#include "main.h"
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
/**
 * _strlen - returns the length of the string
 *
 * @s: character
 *@_strlen: string length
 * Return Always 0
 */
int _strlen(char *s)
{
	int a;

	s = (char *) s;
	a = strlen(s);

	return (a);
}
